# Production environment — committed to repo, read by Vercel at build + runtime
# DO NOT put secrets here — use Vercel dashboard for SUPABASE_SERVICE_ROLE_KEY etc.
NEXT_PUBLIC_APP_URL=https://contractflow-omega.vercel.app
